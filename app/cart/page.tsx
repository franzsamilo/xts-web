"use client";
import { ComingSoonStub } from '@/components/ui/ComingSoonStub';
import { ShoppingCart } from 'lucide-react';

export default function CartPage() {
  return <ComingSoonStub title="Cart" icon={<ShoppingCart className="w-10 h-10" />} />;
}
